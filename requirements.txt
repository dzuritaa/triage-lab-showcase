# Only core/triage.py needs this — it is the one module that calls the model.
# core/retrieve.py and evals/run.py are standard library only, which is why CI
# runs them with no install step and fork PRs need no secret.
anthropic>=0.69.0
